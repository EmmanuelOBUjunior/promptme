import PromptCard from "./PromptCard"


const Profile = (name, handleDelete, desc, data, handleEdit) => {
  return (
    <section className="w-full">
      <h1>
      <span className="orange_gradient">My Profile</h1></span>
    </section>
  )
}

export default Profile